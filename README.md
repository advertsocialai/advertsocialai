# AdvertSocial React + Tailwind Scaffold

This scaffold was generated to convert an existing HTML landing page into a React + Tailwind project.

Quick start (Windows PowerShell):

```powershell
npm install
npm run dev
```

Files of interest:
- `src/pages/LandingPage.jsx` — paste the original HTML here. I can convert it to JSX/Tailwind when you provide it or move the HTML into the project.
- `src/components/*` — header/footer example components.

Next steps I can do for you upon request:
- Convert the full HTML into JSX components and Tailwind classes.
- Break the page into smaller reusable components.
- Add routing if you want multiple pages.
